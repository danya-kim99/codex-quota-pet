Two focused interaction fixes for Apple silicon Macs running macOS 14 or newer.

## Fixed in this release

- The black hole now keeps its exact position when switching apps, windows, or
  Spaces, including when it is placed partly outside a screen corner.
- Transparent panel padding no longer captures pointer input. Hover, drag, and
  context-menu interaction now follow the visible black hole while the existing
  absorption core remains unchanged.

## Privacy and compatibility

- This update introduces no network request, process, dependency, backend,
  analytics, permission, preference, or stored data.
- Existing Smooth/Pixel styles, S/M/L sizes, Standard/Turbo, Reduce Motion,
  authored quota reactions, Object Mix controls, position controls, launch at
  login, and saved local history are preserved.
- The preview remains arm64-only and requires macOS 14 or newer plus a locally
  installed and authenticated Codex CLI.

## Install or update

1. Download and unzip `Black-Hole-Codex-Quota-Indicator-v0.8.2-macos-arm64.zip`.
2. Quit the currently running app if updating.
3. Move `Black Hole Codex Quota Indicator.app` to `/Applications`, replacing
   the previous copy when prompted.
4. Open the app. Saved settings and local quota history are retained.

## Verification

- The complete macOS test suite passed: 97 tests, zero failures, zero skips.
- A clean arm64 Release build was verified as version 0.8.2 (build 20), with a
  macOS 14.0 deployment target and no coverage symbols.
- `script/build_and_run.sh --verify` built and launched the app successfully.
- Built-candidate runtime QA kept an exact partly-offscreen corner frame across
  app and Space notifications, passed transparent padding through, and kept the
  visible disk and accessibility actions interactive.
- `git diff --check` passed.

**Full Changelog**: [v0.8.1...v0.8.2](https://github.com/danya-kim99/codex-quota-pet/compare/v0.8.1...v0.8.2)
